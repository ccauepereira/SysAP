openapi: 3.1.1
info:
  title: API SysAP
  version: 0.2.0
  description: >-
    Contrato HTTP do SysAP. Saude e prontidao estao implementadas; a superficie
    versionada de identidade descreve o contrato planejado para a Fase 2 e nao
    representa funcionalidade disponivel nesta subfase documental.
servers:
  - url: http://127.0.0.1:8080
    description: API local em loopback
security:
  - BearerAccessToken: []
tags:
  - name: Sistema
    description: Estado tecnico do processo e de suas dependencias.
  - name: Convites de atleta
    description: Pre-cadastro protegido e ciclo de ativacao de atletas.
  - name: Autenticacao
    description: Autenticacao, sessao, MFA e recuperacao sem cadastro publico.
  - name: Identidade
    description: Identidade atual e administracao protegida de acesso.
paths:
  /healthz:
    get:
      operationId: getHealth
      tags:
        - Sistema
      summary: Consultar saude do processo
      description: >-
        Confirma que o processo HTTP esta ativo sem consultar PostgreSQL ou
        qualquer outra dependencia externa.
      security: []
      responses:
        "200":
          description: Processo da API em execucao.
          headers:
            Cache-Control:
              $ref: "#/components/headers/CacheControl"
            X-Request-ID:
              $ref: "#/components/headers/RequestID"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthResponse"
              examples:
                healthy:
                  summary: Processo saudavel
                  value:
                    status: ok
                    service: sysap-api
  /readyz:
    get:
      operationId: getReadiness
      tags:
        - Sistema
      summary: Consultar prontidao da API
      description: >-
        Verifica o PostgreSQL com timeout curto. A API continua saudavel quando
        o banco esta indisponivel, mas este endpoint responde 503.
      security: []
      responses:
        "200":
          description: API pronta e PostgreSQL acessivel.
          headers:
            Cache-Control:
              $ref: "#/components/headers/CacheControl"
            X-Request-ID:
              $ref: "#/components/headers/RequestID"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReadinessResponse"
              examples:
                ready:
                  summary: Banco disponivel
                  value:
                    status: ready
                    service: sysap-api
                    checks:
                      database: up
        "503":
          description: API ativa, mas ainda nao pronta.
          headers:
            Cache-Control:
              $ref: "#/components/headers/CacheControl"
            X-Request-ID:
              $ref: "#/components/headers/RequestID"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NotReadyResponse"
              examples:
                databaseUnavailable:
                  summary: Banco indisponivel
                  value:
                    error:
                      code: service_not_ready
                      message: service is not ready
                      request_id: 0123456789abcdef0123456789abcdef
  /v1/organizations/{organization_id}/athlete-invitations:
    post:
      operationId: createAthleteInvitation
      tags:
        - Convites de atleta
      summary: Pre-cadastrar atleta e iniciar ativacao
      description: >-
        Cria um convite na organizacao selecionada por organization_id.
        Requer token de Owner.
      x-sysap-implementation-status: implemented
      parameters:
        - name: organization_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - $ref: "#/components/parameters/IdempotencyKey"
        - $ref: "#/components/parameters/OrganizationContext"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateAthleteInvitationRequest"
            examples:
              fictionalAthlete:
                summary: Atleta ficticio
                value:
                  full_name: "Atleta Exemplo"
                  phone_e164: "+12025550123"
                  email: "atleta@example.com"
                  birth_date: "2010-01-15"
                  locality: "Centro"
                  team_id: 50000000-0000-4000-8000-000000000001
                  football_position: midfielder
                  enrollment_delivery_channel: sms
      responses:
        "201":
          description: Convite criado
          headers:
            Cache-Control:
              $ref: "#/components/headers/NoStore"
            X-Request-ID:
              $ref: "#/components/headers/RequestID"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AthleteInvitationResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/AuthenticationRequired"
        "403":
          $ref: "#/components/responses/AccessDenied"
        "409":
          $ref: "#/components/responses/ResourceConflict"
        "422":
          $ref: "#/components/responses/ValidationFailed"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"
  /v1/activation/start:
    post:
      operationId: startActivation
      tags:
        - Convites de atleta
      summary: Iniciar ativação de atleta
      description: >-
        Inicia o fluxo de ativação, gerando um OTP para a matrícula informada.
        Sempre retorna 202 Accepted para não enumerar matrículas válidas.
        Em desenvolvimento, o provedor é local; em produção, o envio é
        configurado no Supabase Auth. A API não integra provedores SMS diretamente.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ActivationStartRequest"
      responses:
        "202":
          description: Solicitação aceita
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: accepted
        "503":
          $ref: "#/components/responses/IdentityVerificationUnavailable"
  /v1/activation/verify:
    post:
      operationId: verifyActivation
      tags:
        - Convites de atleta
      summary: Verificar OTP
      description: >-
        Verifica se o OTP fornecido corresponde à matrícula e não expirou.
        Retorna uma prova de ativação (activation_proof) que pode ser usada depois.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ActivationVerifyRequest"
      responses:
        "200":
          description: OTP validado com sucesso
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ActivationVerifyResponse"
        "401":
          $ref: "#/components/responses/AuthenticationRequired"
        "503":
          $ref: "#/components/responses/IdentityVerificationUnavailable"
  /v1/activation/verify-sms:
    post:
      operationId: verifyActivationSMS
      tags: [Convites de atleta]
      summary: Confirmar o OTP enviado por SMS
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ActivationVerifyRequest"
      responses:
        "200":
          description: Proof curta entregue somente ao BFF por HTTPS.
          headers:
            Cache-Control:
              $ref: "#/components/headers/NoStore"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ActivationProofResponse"
        "401":
          $ref: "#/components/responses/InvalidCredentials"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"
  /v1/activation/email/start:
    post:
      operationId: startActivationEmail
      tags: [Convites de atleta]
      summary: Iniciar confirmação por e-mail após o SMS
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ActivationEmailStartRequest"
      responses:
        "202":
          $ref: "#/components/responses/CommandAccepted"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"
  /v1/activation/email/verify:
    post:
      operationId: verifyActivationEmail
      tags: [Convites de atleta]
      summary: Confirmar o OTP enviado por e-mail
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ActivationEmailVerifyRequest"
      responses:
        "200":
          description: Proof final entregue somente ao BFF por HTTPS.
          headers:
            Cache-Control:
              $ref: "#/components/headers/NoStore"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ActivationProofResponse"
        "401":
          $ref: "#/components/responses/InvalidCredentials"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"
  /v1/activation/complete:
    post:
      operationId: completeActivation
      tags:
        - Convites de atleta
      summary: Concluir ativação
      description: >-
        Conclui o fluxo de ativação, validando a activation_proof e definindo a senha do atleta.
        Provisiona a identidade pelo adaptador server-side do Supabase Auth.
      x-sysap-implementation-status: implemented
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ActivationCompleteRequest"
      responses:
        "204":
          description: Ativação concluída com sucesso
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/AuthenticationRequired"
        "409":
          $ref: "#/components/responses/ResourceConflict"
        "422":
          $ref: "#/components/responses/ValidationFailed"
        "429":
          $ref: "#/components/responses/RateLimited"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"
  /v1/auth/login:
    post:
      operationId: login
      tags:
        - Autenticacao
      summary: Autenticar por matrícula e senha
      description: >-
        Autentica athlete, trainer ou owner com matrícula e senha. A API é a
        única intermediária com o Supabase Auth; antes de emitir tokens ela
        confirma perfil, organização e membership no estado autoritativo local.
        Matrícula inexistente, senha inválida, suspensão, ativação pendente e
        bloqueio temporário retornam exatamente a mesma resposta 401.
      x-sysap-implementation-status: implemented
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LoginRequest"
      responses:
        "200":
          description: Credenciais aceitas e sessao emitida.
          headers:
            Cache-Control:
              $ref: "#/components/headers/NoStore"
            X-Request-ID:
              $ref: "#/components/headers/RequestID"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LoginResponse"
        "401":
          $ref: "#/components/responses/InvalidCredentials"
  /v1/auth/mfa:
    get:
      operationId: getMFAStatus
      tags: [Autenticacao]
      summary: Consultar o estado MFA do próprio usuário
      description: Confere o fator do próprio usuário sem expor identificador, segredo ou QR code.
      x-sysap-implementation-status: implemented
      responses:
        "200":
          description: Estado local reconciliado com o fator no Supabase Auth.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MFAStatusResponse"
        "401":
          $ref: "#/components/responses/AuthenticationRequired"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"
  /v1/auth/mfa/enroll:
    post:
      operationId: enrollMFA
      tags:
        - Autenticacao
      summary: Iniciar enrollment TOTP do próprio staff
      description: >-
        Requer sessão autenticada AAL1 do próprio owner ou trainer. A URI contém
        segredo TOTP e existe somente nesta resposta HTTPS com no-store.
      x-sysap-implementation-status: implemented
      responses:
        "200":
          description: Fator pendente e URI de provisionamento, sem persistência local do segredo.
          headers:
            Cache-Control:
              $ref: "#/components/headers/NoStore"
            X-Request-ID:
              $ref: "#/components/headers/RequestID"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MFAEnrollmentResponse"
        "401":
          $ref: "#/components/responses/AuthenticationRequired"
        "403":
          $ref: "#/components/responses/MFARequired"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"
  /v1/auth/mfa/challenge:
    post:
      operationId: createMFAChallenge
      tags: [Autenticacao]
      summary: Criar challenge TOTP do próprio fator
      description: Cria um challenge opaco no Supabase Auth para a verificação TOTP imediata.
      x-sysap-implementation-status: implemented
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MFAFactorRequest"
      responses:
        "200":
          description: Challenge opaco usado apenas para a verificação imediata.
          headers:
            Cache-Control:
              $ref: "#/components/headers/NoStore"
            X-Request-ID:
              $ref: "#/components/headers/RequestID"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MFAChallengeResponse"
        "401":
          $ref: "#/components/responses/InvalidCredentials"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"
  /v1/auth/mfa/verify:
    post:
      operationId: verifyMFAChallenge
      tags: [Autenticacao]
      summary: Verificar challenge TOTP e elevar a sessão a AAL2
      description: Verifica o código TOTP pelo adaptador server-side e atualiza somente a sessão local corrente.
      x-sysap-implementation-status: implemented
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MFAVerifyRequest"
      responses:
        "200":
          description: Sessão elevada; tokens existem somente nesta resposta HTTPS.
          headers:
            Cache-Control:
              $ref: "#/components/headers/NoStore"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionResponse"
        "401":
          $ref: "#/components/responses/InvalidCredentials"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"
  /v1/athlete-invitations/{invitation_id}/resend:
    parameters:
      - $ref: "#/components/parameters/InvitationID"
    post:
      operationId: resendAthleteInvitation
      tags:
        - Convites de atleta
      summary: Solicitar novo OTP de ativacao
      description: >-
        Aceita novo envio somente para convite previamente provisionado,
        visivel na organizacao e em estado compativel. A resposta nao revela
        telefone completo, fornecedor ou resultado individual da entrega.
      x-sysap-implementation-status: planned
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "202":
          $ref: "#/components/responses/CommandAccepted"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/AuthenticationRequired"
        "403":
          $ref: "#/components/responses/AccessDenied"
        "404":
          $ref: "#/components/responses/ResourceNotFound"
        "409":
          $ref: "#/components/responses/ResourceConflict"
        "429":
          $ref: "#/components/responses/RateLimited"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"
  /v1/athlete-invitations/{invitation_id}:
    parameters:
      - $ref: "#/components/parameters/InvitationID"
    delete:
      operationId: cancelAthleteInvitation
      tags:
        - Convites de atleta
      summary: Cancelar convite de atleta
      description: >-
        Fecha um convite ainda cancelavel na organizacao do ator. Convites
        inexistentes e pertencentes a outra organizacao produzem a mesma
        resposta publica de recurso nao encontrado.
      x-sysap-implementation-status: planned
      responses:
        "204":
          $ref: "#/components/responses/CommandCompleted"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/AuthenticationRequired"
        "403":
          $ref: "#/components/responses/AccessDenied"
        "404":
          $ref: "#/components/responses/ResourceNotFound"
        "409":
          $ref: "#/components/responses/ResourceConflict"
        "429":
          $ref: "#/components/responses/RateLimited"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"
  /v1/auth/refresh:
    post:
      operationId: refreshSession
      tags:
        - Autenticacao
      summary: Renovar sessao
      description: >-
        Troca um refresh token valido por um novo par rotacionado. O cliente
        serializa a operacao por sessao e substitui o par atomicamente; a API
        nao persiste resposta idempotente contendo tokens. O token no corpo e
        uma credencial e nunca deve ser registrado. Falhas nao informam se a
        sessao expirou, foi revogada ou reutilizada.
      x-sysap-implementation-status: implemented
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RefreshSessionRequest"
      responses:
        "200":
          description: Sessao renovada e tokens anteriores rotacionados.
          headers:
            Cache-Control:
              $ref: "#/components/headers/NoStore"
            X-Request-ID:
              $ref: "#/components/headers/RequestID"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionResponse"
        "401":
          $ref: "#/components/responses/InvalidCredentials"
  /v1/auth/logout:
    post:
      operationId: logoutSession
      tags:
        - Autenticacao
      summary: Encerrar sessao atual
      description: >-
        Marca o session_id verificado como revogado no SysAP antes de solicitar
        sign-out ao Auth. Toda rota protegida consulta essa revogacao porque o
        access JWT pode verificar ate exp. O Web remove cookies no BFF e o
        mobile remove material local mesmo se a resposta remota for ambigua.
      x-sysap-implementation-status: implemented
      responses:
        "204":
          $ref: "#/components/responses/CommandCompleted"
        "401":
          $ref: "#/components/responses/AuthenticationRequired"
  /v1/auth/logout-all:
    post:
      operationId: logoutAllSessions
      tags:
        - Autenticacao
      summary: Encerrar todas as sessoes
      description: >-
        Grava um corte local e revoga todas as sessoes conhecidas do sujeito
        autenticado antes de solicitar sign-out global, sem aceitar usuario,
        organizacao ou papel do cliente. O primeiro sucesso retorna 204; retry
        com o mesmo bearer ja revogado pode retornar 401, que o cliente trata
        como estado terminal e ainda assim remove seu material local.
      x-sysap-implementation-status: implemented
      responses:
        "204":
          $ref: "#/components/responses/CommandCompleted"
        "401":
          $ref: "#/components/responses/AuthenticationRequired"
  /v1/auth/password-recovery/start:
    post:
      operationId: startPasswordRecovery
      tags:
        - Autenticacao
      summary: Iniciar recuperação de senha sem enumeração
      description: >-
        Retorna sempre 202 com corpo neutro para matrícula existente ou não.
        O canal verificado é resolvido internamente pelo Supabase Auth e nunca
        aparece na resposta, no log ou no banco SysAP.
      x-sysap-implementation-status: implemented
      security: []
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PasswordRecoveryStartRequest"
      responses:
        "202":
          $ref: "#/components/responses/CommandAccepted"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"
  /v1/auth/password-recovery/verify:
    post:
      operationId: verifyPasswordRecovery
      tags:
        - Autenticacao
      summary: Verificar OTP de recuperação
      description: >-
        O OTP é verificado somente pelo adaptador do Supabase Auth. O resultado
        positivo emite uma proof aleatória de uso único, persistida apenas como HMAC.
      x-sysap-implementation-status: implemented
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PasswordRecoveryVerifyRequest"
      responses:
        "200":
          description: Proof opaca para a conclusão imediata.
          headers:
            Cache-Control:
              $ref: "#/components/headers/NoStore"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PasswordRecoveryProofResponse"
        "401":
          $ref: "#/components/responses/InvalidCredentials"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"
  /v1/auth/password-recovery/complete:
    post:
      operationId: completePasswordRecovery
      tags: [Autenticacao]
      summary: Concluir recuperação e revogar sessões locais
      description: >-
        Exige proof de uso único e senha válida. Owner ou trainer com MFA ativo
        recebem mfa_required: recuperação administrativa assistida é externa a esta subfase.
      x-sysap-implementation-status: implemented
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PasswordRecoveryCompleteRequest"
      responses:
        "204":
          $ref: "#/components/responses/CommandCompleted"
        "401":
          $ref: "#/components/responses/InvalidCredentials"
        "403":
          $ref: "#/components/responses/MFARequired"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"
  /v1/me:
    get:
      operationId: getCurrentIdentity
      tags:
        - Identidade
      summary: Consultar identidade atual
      description: >-
        Retorna perfil e memberships confirmadas no estado autoritativo do
        SysAP. O resultado nao expoe identificador tecnico de autenticacao,
        telefone, tokens nem claims usados como unica fonte de autorizacao.
      x-sysap-implementation-status: planned
      security:
        - BearerAccessToken: []
      responses:
        "200":
          description: Identidade e vinculos atuais.
          headers:
            Cache-Control:
              $ref: "#/components/headers/NoStore"
            X-Request-ID:
              $ref: "#/components/headers/RequestID"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CurrentIdentityResponse"
              examples:
                fictionalAthlete:
                  summary: Perfil ficticio de atleta
                  value:
                    profile:
                      id: 40000000-0000-4000-8000-000000000001
                      display_name: Atleta Exemplo
                    memberships:
                      - organization_id: 30000000-0000-4000-8000-000000000001
                        role: athlete
                        status: active
        "401":
          $ref: "#/components/responses/AuthenticationRequired"
        "403":
          $ref: "#/components/responses/AccessDenied"
        "429":
          $ref: "#/components/responses/RateLimited"
        "503":
          $ref: "#/components/responses/IdentityVerificationUnavailable"
  /v1/memberships/{membership_id}/access:
    parameters:
      - $ref: "#/components/parameters/MembershipID"
    patch:
      operationId: updateMembershipAccess
      tags:
        - Identidade
      summary: Suspender ou reativar acesso
      description: >-
        Define o estado de acesso de uma membership visivel na mesma organizacao.
        Papel, organizacao e identidade tecnica nao podem ser alterados por este
        corpo. Recurso inexistente e recurso de outro tenant retornam o mesmo 404.
      x-sysap-implementation-status: planned
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateMembershipAccessRequest"
            examples:
              suspendForReview:
                summary: Suspensao com razao segura
                value:
                  access_status: suspended
                  reason_code: security_review
      responses:
        "200":
          description: Estado de acesso atualizado.
          headers:
            Cache-Control:
              $ref: "#/components/headers/NoStore"
            X-Request-ID:
              $ref: "#/components/headers/RequestID"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MembershipAccessResponse"
              examples:
                suspended:
                  summary: Membership ficticia suspensa
                  value:
                    membership_id: 20000000-0000-4000-8000-000000000001
                    access_status: suspended
                    updated_at: "2026-07-22T15:10:00Z"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/AuthenticationRequired"
        "403":
          $ref: "#/components/responses/AccessDenied"
        "404":
          $ref: "#/components/responses/ResourceNotFound"
        "409":
          $ref: "#/components/responses/ResourceConflict"
        "422":
          $ref: "#/components/responses/ValidationFailed"
        "429":
          $ref: "#/components/responses/RateLimited"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"
components:
  securitySchemes:
    BearerAccessToken:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: >-
        Access token de curta duracao validado pela API. O token e tratado pelo
        cliente como opaco; sub/session_id nao sao IDs de negocio. Sessao deve
        ter sido registrada pela API; papel, organizacao e acesso sao
        confirmados no estado do SysAP.
  parameters:
    IdempotencyKey:
      name: Idempotency-Key
      in: header
      required: true
      description: >-
        UUID gerado pelo cliente para tornar o comando repetivel. Comandos sem
        segredo comparam somente a projecao canonica nao secreta. Em ativacao e
        recovery, desafio/estado terminal controlam repeticao; senha, OTP/TOTP,
        token, ticket MFA e qualquer derivado nunca entram no fingerprint.
      schema:
        type: string
        format: uuid
      example: 00000000-0000-4000-8000-000000000001
    OrganizationContext:
      name: X-Organization-ID
      in: header
      required: true
      description: >-
        Seletor sujeito a autorizacao, obrigatorio somente para endpoints
        tenant-bound futuros. Nao e exigido em /healthz, /readyz ou futuro
        /v1/me. Um formato invalido ou malformado produz invalid_request.
      schema:
        type: string
        format: uuid
      example: 30000000-0000-4000-8000-000000000001
    InvitationID:
      name: invitation_id
      in: path
      required: true
      description: Identificador opaco do convite no SysAP.
      schema:
        type: string
        format: uuid
      example: 10000000-0000-4000-8000-000000000001
    MembershipID:
      name: membership_id
      in: path
      required: true
      description: Identificador opaco da membership no SysAP.
      schema:
        type: string
        format: uuid
      example: 20000000-0000-4000-8000-000000000001
  responses:
    CommandAccepted:
      description: Comando aceito sem confirmar existencia, destino ou entrega.
      headers:
        Cache-Control:
          $ref: "#/components/headers/NoStore"
        X-Request-ID:
          $ref: "#/components/headers/RequestID"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/CommandAcceptedResponse"
          examples:
            accepted:
              summary: Comando aceito
              value:
                status: accepted
    CommandCompleted:
      description: Comando concluido sem corpo de resposta.
      headers:
        Cache-Control:
          $ref: "#/components/headers/NoStore"
        X-Request-ID:
          $ref: "#/components/headers/RequestID"
    BadRequest:
      description: Requisicao malformada ou header invalido.
      headers:
        Cache-Control:
          $ref: "#/components/headers/NoStore"
        X-Request-ID:
          $ref: "#/components/headers/RequestID"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
          examples:
            malformedRequest:
              summary: Requisicao malformada
              value:
                error:
                  code: invalid_request
                  message: request is invalid
                  request_id: 0123456789abcdef0123456789abcdef
    InvalidCredentials:
      description: Falha generica de autenticacao sem indicar qual dado falhou.
      headers:
        Cache-Control:
          $ref: "#/components/headers/NoStore"
        X-Request-ID:
          $ref: "#/components/headers/RequestID"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
          examples:
            authenticationFailed:
              summary: Autenticacao nao concluida
              value:
                error:
                  code: invalid_credentials
                  message: invalid credentials
                  request_id: 0123456789abcdef0123456789abcdef
    MFARequired:
      description: Owner ou trainer autenticado precisa elevar a sessão a AAL2.
      headers:
        Cache-Control:
          $ref: "#/components/headers/NoStore"
        X-Request-ID:
          $ref: "#/components/headers/RequestID"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
          examples:
            mfaRequired:
              value:
                error:
                  code: mfa_required
                  message: multi-factor authentication is required
                  request_id: 0123456789abcdef0123456789abcdef
    AuthenticationRequired:
      description: >-
        Access token ausente, invalido ou expirado, ou sessao local inexistente
        ou revogada.
      headers:
        Cache-Control:
          $ref: "#/components/headers/NoStore"
        WWW-Authenticate:
          $ref: "#/components/headers/BearerChallenge"
        X-Request-ID:
          $ref: "#/components/headers/RequestID"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
          examples:
            authenticationRequired:
              summary: Autenticacao necessaria
              value:
                error:
                  code: authentication_required
                  message: authentication is required
                  request_id: 0123456789abcdef0123456789abcdef
    AccessDenied:
      description: Sujeito autenticado sem acesso ativo ou permissao suficiente.
      headers:
        Cache-Control:
          $ref: "#/components/headers/NoStore"
        X-Request-ID:
          $ref: "#/components/headers/RequestID"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
          examples:
            accessDenied:
              summary: Acesso negado
              value:
                error:
                  code: access_denied
                  message: access is denied
                  request_id: 0123456789abcdef0123456789abcdef
    IdentityVerificationUnavailable:
      description: >-
        Nenhuma chave JWKS confiavel esta disponivel para verificacao de
        identidade. Esta resposta e planejada para GET /v1/me e nao revela
        detalhes do token, claim, emissor ou infraestrutura.
      headers:
        Cache-Control:
          $ref: "#/components/headers/NoStore"
        X-Request-ID:
          $ref: "#/components/headers/RequestID"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
          examples:
            identityVerificationUnavailable:
              summary: Verificacao de identidade indisponivel
              value:
                error:
                  code: identity_verification_unavailable
                  message: identity verification is temporarily unavailable
                  request_id: 0123456789abcdef0123456789abcdef
    ResourceNotFound:
      description: >-
        Recurso inexistente ou fora da organizacao visivel, sem distinguir os
        dois casos.
      headers:
        Cache-Control:
          $ref: "#/components/headers/NoStore"
        X-Request-ID:
          $ref: "#/components/headers/RequestID"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
          examples:
            resourceNotFound:
              summary: Recurso nao encontrado
              value:
                error:
                  code: resource_not_found
                  message: resource was not found
                  request_id: 0123456789abcdef0123456789abcdef
    ResourceConflict:
      description: Estado incompativel ou chave de idempotencia reutilizada.
      headers:
        Cache-Control:
          $ref: "#/components/headers/NoStore"
        X-Request-ID:
          $ref: "#/components/headers/RequestID"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
          examples:
            resourceConflict:
              summary: Conflito de estado
              value:
                error:
                  code: resource_conflict
                  message: request conflicts with current state
                  request_id: 0123456789abcdef0123456789abcdef
    ValidationFailed:
      description: Campos invalidos sem ecoar valores sensiveis recebidos.
      headers:
        Cache-Control:
          $ref: "#/components/headers/NoStore"
        X-Request-ID:
          $ref: "#/components/headers/RequestID"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ValidationErrorResponse"
          examples:
            validationFailed:
              summary: Falha de validacao estatica
              value:
                error:
                  code: validation_failed
                  message: request validation failed
                  request_id: 0123456789abcdef0123456789abcdef
                  details:
                    - field: enrollment_number
                      reason: invalid_format
    RateLimited:
      description: Limite temporario excedido sem revelar a dimensao aplicada.
      headers:
        Cache-Control:
          $ref: "#/components/headers/NoStore"
        Retry-After:
          $ref: "#/components/headers/RetryAfter"
        X-Request-ID:
          $ref: "#/components/headers/RequestID"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
          examples:
            rateLimited:
              summary: Limite temporario excedido
              value:
                error:
                  code: rate_limited
                  message: too many requests
                  request_id: 0123456789abcdef0123456789abcdef
    ServiceUnavailable:
      description: >-
        Operacao temporariamente indisponivel sem identificar dependencia ou
        fornecedor. Em fluxos antienumeracao, somente falha global usa 503.
      headers:
        Cache-Control:
          $ref: "#/components/headers/NoStore"
        Retry-After:
          $ref: "#/components/headers/RetryAfter"
        X-Request-ID:
          $ref: "#/components/headers/RequestID"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
          examples:
            serviceUnavailable:
              summary: Servico temporariamente indisponivel
              value:
                error:
                  code: service_unavailable
                  message: service is temporarily unavailable
                  request_id: 0123456789abcdef0123456789abcdef
  headers:
    CacheControl:
      description: Impede cache das respostas de saude e prontidao.
      required: true
      schema:
        type: string
        const: no-store
      example: no-store
    RequestID:
      description: >-
        Identificador hexadecimal gerado pelo servidor para correlacao da
        requisicao. O caminho normal possui 32 caracteres; a contingencia pode
        produzir um valor hexadecimal maior.
      required: true
      schema:
        $ref: "#/components/schemas/RequestID"
      example: 0123456789abcdef0123456789abcdef
    NoStore:
      description: Impede armazenamento de respostas de identidade e autenticacao.
      required: true
      schema:
        type: string
        const: no-store
      example: no-store
    RetryAfter:
      description: Segundos inteiros antes de uma nova tentativa permitida.
      required: true
      schema:
        type: integer
        minimum: 1
        maximum: 86400
      example: 60
    BearerChallenge:
      description: Desafio padrao para access token bearer.
      required: true
      schema:
        type: string
        const: Bearer
      example: Bearer
    InvitationLocation:
      description: Caminho relativo do convite criado.
      required: true
      schema:
        type: string
        pattern: "^/v1/athlete-invitations/[0-9a-f-]{36}$"
      example: /v1/athlete-invitations/10000000-0000-4000-8000-000000000001
  schemas:
    RequestID:
      type: string
      pattern: "^[0-9a-f]+$"
      minLength: 32
      description: Identificador hexadecimal opaco gerado pelo servidor.
    HealthResponse:
      type: object
      additionalProperties: false
      required:
        - status
        - service
      properties:
        status:
          type: string
          const: ok
        service:
          type: string
          const: sysap-api
    ReadinessResponse:
      type: object
      additionalProperties: false
      required:
        - status
        - service
        - checks
      properties:
        status:
          type: string
          const: ready
        service:
          type: string
          const: sysap-api
        checks:
          $ref: "#/components/schemas/ReadinessChecks"
    ReadinessChecks:
      type: object
      additionalProperties: false
      required:
        - database
      properties:
        database:
          type: string
          const: up
    NotReadyResponse:
      type: object
      additionalProperties: false
      required:
        - error
      properties:
        error:
          $ref: "#/components/schemas/NotReadyError"
    NotReadyError:
      type: object
      additionalProperties: false
      required:
        - code
        - message
        - request_id
      properties:
        code:
          type: string
          const: service_not_ready
        message:
          type: string
          const: service is not ready
        request_id:
          $ref: "#/components/schemas/RequestID"
    UUID:
      type: string
      format: uuid
      description: Identificador opaco no dominio do SysAP.
    EnrollmentNumber:
      type: string
      minLength: 10
      maxLength: 10
      pattern: "^[0-9]{10}$"
      description: Matricula publica ficticia de exatamente dez digitos.
      example: "2026000001"
    FullNameInput:
      type: string
      minLength: 2
      maxLength: 120
      description: Nome necessario ao pre-cadastro, sem uso como credencial.
      example: Atleta Exemplo
    BirthDateInput:
      type: string
      format: date
      description: >-
        Data civil de nascimento ficticia, validada para nao aceitar data
        futura. Nao e usada para gerar matricula ou credencial.
      example: "2010-01-15"
    PhoneE164Input:
      type: string
      minLength: 9
      maxLength: 16
      pattern: "^\\+[1-9][0-9]{7,14}$"
      writeOnly: true
      description: >-
        Telefone internacional recebido apenas no pre-cadastro protegido. O
        exemplo pertence a faixa reservada para documentacao.
      example: "+12025550123"
    PasswordInput:
      type: string
      minLength: 15
      maxLength: 128
      writeOnly: true
      description: >-
        Senha escolhida pelo usuario. Espacos e Unicode sao permitidos, sem
        truncamento silencioso nem regra previsivel de composicao.
    OTPInput:
      type: string
      minLength: 6
      maxLength: 6
      pattern: "^[0-9]{6}$"
      writeOnly: true
      description: OTP numerico de seis digitos, tratado como segredo de uso unico.
    TOTPInput:
      type: string
      minLength: 6
      maxLength: 6
      pattern: "^[0-9]{6}$"
      writeOnly: true
      description: Codigo TOTP numerico de seis digitos, tratado como segredo.
    RefreshTokenInput:
      type: string
      minLength: 1
      maxLength: 16384
      writeOnly: true
      description: Refresh token opaco recebido somente para renovacao.
    AccessTokenOutput:
      type: string
      minLength: 1
      maxLength: 16384
      readOnly: true
      description: Access token de curta duracao retornado somente apos autenticacao.
    RefreshTokenOutput:
      type: string
      minLength: 1
      maxLength: 16384
      readOnly: true
      description: Refresh token rotacionavel retornado somente apos autenticacao.
    TOTPProvisioningURIOutput:
      type: string
      format: uri
      minLength: 1
      maxLength: 2048
      readOnly: true
      description: >-
        URI otpauth de enrollment que contem o segredo TOTP. E exibida somente
        ao proprio staff pelo BFF, nunca registrada, persistida ou reutilizada.
    InvitationStatus:
      type: string
      enum:
        - pending_provisioning
        - provisioning_failed
        - pending_activation
        - activation_finalizing
        - accepted
        - cancelled
        - expired
      description: Estado persistido do convite, separado do acesso da membership.
    MembershipRole:
      type: string
      enum:
        - owner
        - trainer
        - athlete
      description: Papel confirmado no estado autoritativo do SysAP.
    MembershipAccessStatus:
      type: string
      enum:
        - active
        - suspended
      description: >-
        Projecao mutavel de acesso. Membership pending_activation ou closed nao
        aparece em /me e nao pode ser alvo deste PATCH.
    CreateAthleteInvitationRequest:
      type: object
      additionalProperties: false
      required:
        - full_name
        - phone_e164
        - email
        - birth_date
        - locality
        - team_id
        - football_position
        - enrollment_delivery_channel
      properties:
        full_name:
          type: string
          minLength: 2
          maxLength: 120
        display_name:
          type: string
          minLength: 1
          description: Nome de exibicao do atleta
        phone_e164:
          type: string
          pattern: '^\+[1-9]\d{1,14}$'
          description: Telefone normalizado em formato E.164
        email:
          type: string
          format: email
        birth_date:
          type: string
          format: date
        locality:
          type: string
          minLength: 2
          maxLength: 120
        team_id:
          $ref: "#/components/schemas/UUID"
        football_position:
          type: string
          enum: [goalkeeper, defender, midfielder, forward]
        enrollment_delivery_channel:
          type: string
          enum: [sms, email]
    AthleteInvitationResponse:
      type: object
      additionalProperties: false
      required:
        - invitation_id
        - profile_id
        - status
        - expires_at
        - delivery_channel
      properties:
        invitation_id:
          $ref: "#/components/schemas/UUID"
        profile_id:
          $ref: "#/components/schemas/UUID"
        status:
          type: string
          enum: [pending_activation]
          description: O atleta deve ativar a conta com senha e OTP.
        expires_at:
          type: string
          format: date-time
          description: Data de expiração do convite.
        delivery_channel:
          type: string
          enum: [sms, email]
    ActivationStartRequest:
      type: object
      additionalProperties: false
      required:
        - enrollment_number
      properties:
        enrollment_number:
          $ref: "#/components/schemas/EnrollmentNumber"
    ActivationVerifyRequest:
      type: object
      additionalProperties: false
      required:
        - enrollment_number
        - code
      properties:
        enrollment_number:
          $ref: "#/components/schemas/EnrollmentNumber"
        code:
          $ref: "#/components/schemas/OTPInput"
    ActivationVerifyResponse:
      type: object
      additionalProperties: false
      required:
        - activation_proof
      properties:
        activation_proof:
          type: string
          description: Prova de validação do OTP (hexadecimal, única, opaca)
    ActivationProofResponse:
      type: object
      additionalProperties: false
      required: [activation_proof]
      properties:
        activation_proof:
          type: string
          minLength: 48
          maxLength: 48
          writeOnly: true
          description: Proof opaca retornada somente ao BFF por HTTPS.
    ActivationEmailStartRequest:
      type: object
      additionalProperties: false
      required: [enrollment_number, sms_proof]
      properties:
        enrollment_number:
          $ref: "#/components/schemas/EnrollmentNumber"
        sms_proof:
          type: string
          minLength: 48
          maxLength: 48
          writeOnly: true
    ActivationEmailVerifyRequest:
      type: object
      additionalProperties: false
      required: [enrollment_number, code, sms_proof]
      properties:
        enrollment_number:
          $ref: "#/components/schemas/EnrollmentNumber"
        code:
          $ref: "#/components/schemas/OTPInput"
        sms_proof:
          type: string
          minLength: 48
          maxLength: 48
          writeOnly: true
    ActivationCompleteRequest:
      type: object
      required:
        - activation_proof
        - password
      properties:
        activation_proof:
          type: string
          description: Prova obtida no step de verify
        password:
          type: string
          format: password
          description: Senha do atleta
    ActivationCompleteResponse:
      type: object
      properties:
        profile_id:
          type: string
          format: uuid
        enrollment_number:
          type: string
        role:
          type: string
          example: athlete
        organization_id:
          type: string
          format: uuid
    LoginRequest:
      type: object
      additionalProperties: false
      required:
        - enrollment_number
        - password
      properties:
        enrollment_number:
          $ref: "#/components/schemas/EnrollmentNumber"
        password:
          $ref: "#/components/schemas/PasswordInput"
    LoginResponse:
      type: object
      additionalProperties: false
      required:
        - session_id
        - profile_id
        - organization_id
        - role
        - aal
        - access_token
        - refresh_token
        - expires_in
      properties:
        session_id:
          $ref: "#/components/schemas/UUID"
        profile_id:
          $ref: "#/components/schemas/UUID"
        organization_id:
          $ref: "#/components/schemas/UUID"
        role:
          $ref: "#/components/schemas/MembershipRole"
        aal:
          type: string
          const: aal1
        access_token:
          $ref: "#/components/schemas/AccessTokenOutput"
        refresh_token:
          $ref: "#/components/schemas/RefreshTokenOutput"
        expires_in:
          type: integer
          minimum: 1
    MFAEnrollmentResponse:
      type: object
      additionalProperties: false
      required:
        - factor_id
        - provisioning_uri
      properties:
        factor_id:
          $ref: "#/components/schemas/UUID"
        provisioning_uri:
          $ref: "#/components/schemas/TOTPProvisioningURIOutput"
    MFAStatusResponse:
      type: object
      additionalProperties: false
      required:
        - mfa_enabled
      properties:
        mfa_enabled:
          type: boolean
    MFAFactorRequest:
      type: object
      additionalProperties: false
      required: [factor_id]
      properties:
        factor_id:
          $ref: "#/components/schemas/UUID"
    MFAChallengeResponse:
      type: object
      additionalProperties: false
      required: [challenge_id]
      properties:
        challenge_id:
          type: string
          minLength: 1
          maxLength: 512
          readOnly: true
          description: Challenge opaco retornado somente na resposta HTTPS com no-store.
    MFAVerifyRequest:
      type: object
      additionalProperties: false
      required: [factor_id, challenge_id, code]
      properties:
        factor_id:
          $ref: "#/components/schemas/UUID"
        challenge_id:
          type: string
          minLength: 1
          maxLength: 512
          writeOnly: true
        code:
          $ref: "#/components/schemas/TOTPInput"
    SessionResponse:
      type: object
      additionalProperties: false
      required:
        - access_token
        - refresh_token
        - token_type
        - expires_in
      properties:
        access_token:
          $ref: "#/components/schemas/AccessTokenOutput"
        refresh_token:
          $ref: "#/components/schemas/RefreshTokenOutput"
        token_type:
          type: string
          const: Bearer
          readOnly: true
        expires_in:
          type: integer
          minimum: 1
          maximum: 604800
          readOnly: true
          description: Validade do access token em segundos.
    RefreshSessionRequest:
      type: object
      additionalProperties: false
      required:
        - refresh_token
      properties:
        refresh_token:
          $ref: "#/components/schemas/RefreshTokenInput"
    PasswordRecoveryStartRequest:
      type: object
      additionalProperties: false
      required:
        - enrollment_number
      properties:
        enrollment_number:
          $ref: "#/components/schemas/EnrollmentNumber"
    PasswordRecoveryVerifyRequest:
      type: object
      additionalProperties: false
      required:
        - enrollment_number
        - code
      properties:
        enrollment_number:
          $ref: "#/components/schemas/EnrollmentNumber"
        code:
          $ref: "#/components/schemas/OTPInput"
    PasswordRecoveryProofResponse:
      type: object
      additionalProperties: false
      required: [proof]
      properties:
        proof:
          type: string
          minLength: 43
          maxLength: 256
          readOnly: true
          description: Proof opaca de uso único retornada somente com no-store.
    PasswordRecoveryCompleteRequest:
      type: object
      additionalProperties: false
      required: [proof, password]
      properties:
        proof:
          type: string
          minLength: 43
          maxLength: 256
          writeOnly: true
        password:
          $ref: "#/components/schemas/PasswordInput"
    CommandAcceptedResponse:
      type: object
      additionalProperties: false
      required:
        - status
      properties:
        status:
          type: string
          const: accepted
    CurrentIdentityResponse:
      type: object
      additionalProperties: false
      required:
        - profile
        - memberships
      properties:
        profile:
          $ref: "#/components/schemas/CurrentProfile"
        memberships:
          type: array
          minItems: 1
          maxItems: 100
          items:
            $ref: "#/components/schemas/CurrentMembership"
    CurrentProfile:
      type: object
      additionalProperties: false
      required:
        - id
        - display_name
      properties:
        id:
          $ref: "#/components/schemas/UUID"
        display_name:
          type: string
          minLength: 1
          maxLength: 120
    CurrentMembership:
      type: object
      additionalProperties: false
      required:
        - organization_id
        - role
        - status
      properties:
        organization_id:
          $ref: "#/components/schemas/UUID"
        role:
          $ref: "#/components/schemas/MembershipRole"
        status:
          $ref: "#/components/schemas/MembershipAccessStatus"
    UpdateMembershipAccessRequest:
      oneOf:
        - type: object
          additionalProperties: false
          required:
            - access_status
            - reason_code
          properties:
            access_status:
              type: string
              const: suspended
            reason_code:
              type: string
              enum:
                - security_review
                - policy_violation
                - administrative_suspension
              description: Codigo enumerado e sem dado pessoal para suspensao.
        - type: object
          additionalProperties: false
          required:
            - access_status
            - reason_code
          properties:
            access_status:
              type: string
              const: active
            reason_code:
              type: string
              const: access_restored
              description: Codigo unico permitido para reativacao.
    MembershipAccessResponse:
      type: object
      additionalProperties: false
      required:
        - membership_id
        - access_status
        - updated_at
      properties:
        membership_id:
          $ref: "#/components/schemas/UUID"
        access_status:
          $ref: "#/components/schemas/MembershipAccessStatus"
        updated_at:
          type: string
          format: date-time
          description: Instante UTC da alteracao.
    ErrorResponse:
      type: object
      additionalProperties: false
      required:
        - error
      properties:
        error:
          $ref: "#/components/schemas/PublicError"
    PublicError:
      type: object
      additionalProperties: false
      required:
        - code
        - message
        - request_id
      properties:
        code:
          type: string
          minLength: 3
          maxLength: 64
          pattern: "^[a-z][a-z0-9_]*$"
        message:
          type: string
          minLength: 1
          maxLength: 200
        request_id:
          $ref: "#/components/schemas/RequestID"
    ValidationErrorResponse:
      type: object
      additionalProperties: false
      required:
        - error
      properties:
        error:
          $ref: "#/components/schemas/ValidationError"
    ValidationError:
      type: object
      additionalProperties: false
      required:
        - code
        - message
        - request_id
        - details
      properties:
        code:
          type: string
          const: validation_failed
        message:
          type: string
          const: request validation failed
        request_id:
          $ref: "#/components/schemas/RequestID"
        details:
          type: array
          minItems: 1
          maxItems: 50
          items:
            $ref: "#/components/schemas/ValidationDetail"
    ValidationDetail:
      type: object
      additionalProperties: false
      required:
        - field
        - reason
      properties:
        field:
          type: string
          minLength: 1
          maxLength: 64
          pattern: "^[a-z][a-z0-9_.]*$"
        reason:
          type: string
          enum:
            - required
            - invalid_format
            - too_short
            - too_long
            - not_allowed
            - password_rejected
